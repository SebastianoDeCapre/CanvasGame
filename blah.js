if (phase = 1) {
    if (frameTimer % 160 === 0) {
        createShooter()
    }
} else if (phase = 1) {
    if (frameTimer % 120 === 0) {
        createShooter()
    }
} else if (phase = 1) {
    if (frameTimer % 60 === 0) {
        createShooter()
    }
} 